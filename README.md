# drawerjs

Drawer for Dynamics CRM

```
controllDrawer({
            drawer: {
                title: title,
                buttons: buttons,
                size: size,
                direction: direction,
                baseUrl: '',
                webresource: url + "?Data=" + encodeURIComponent(args),
            }
        })
```
